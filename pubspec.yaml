name: here
description: A new social networking application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0" # Slightly bumped for better modern plugin support
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  http: ^1.2.1
  flutter_map: ^6.1.2
  latlong2: ^0.9.0
  image_picker: ^1.1.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0 # Updated for compatibility with modern Flutter
  cached_network_image: ^3.4.1
  pull_to_refresh: ^2.0.0
  flutter_svg: ^2.0.10+1
  timeline_tile: ^2.0.0
  flutter_markdown: ^0.7.3 # Modern stable version
  
  # --- UPDATED FIREBASE SECTION ---
  firebase_core: ^3.10.1
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.2
  firebase_storage: ^12.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  
  assets:
    - images/
